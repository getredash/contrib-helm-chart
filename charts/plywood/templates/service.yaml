apiVersion: v1
kind: Service
metadata:
  name: {{ include "plywood.fullname" . }}
  labels:
    {{- include "plywood.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plywood.selectorLabels" . | nindent 4 }}
